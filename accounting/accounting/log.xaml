<Page x:Class="accounting.log"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:accounting"
      mc:Ignorable="d" 
      Height="811" Width="1030"
      Title="log">
    <Grid>
        <Grid.RowDefinitions>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <DataGrid x:Name="UsersDataGrid"
              AutoGenerateColumns="False"
              IsReadOnly="True"
              Background="#282828"
              Foreground="Black"
              BorderBrush="#8A2BE2"
              GridLinesVisibility="Vertical"
              AlternatingRowBackground="#333333"
               FontSize="22">
        <DataGrid.Resources>
            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#6A0DAD"/>
            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="White"/>
        </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Пользователь" Binding="{Binding Пользователи.Логин}" Foreground="Black"/>
            <DataGridTextColumn Header="Дата" Binding="{Binding Дата}" Foreground="Black"/>
                <DataGridTextColumn Header="Действие" Binding="{Binding Действие}" Foreground="Black"/>
            </DataGrid.Columns>
    </DataGrid>

    <Grid Grid.Row="1" Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        </Grid>
    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,10" />
        <Button Content="Выйти" Background="#8A2BE2" HorizontalAlignment="Left" Margin="904,0,0,0" VerticalAlignment="Top" Click="Button_Click" FontWeight="Bold" Height="39" Width="126" FontSize="20"/>
    </Grid>
</Page>
